# Asicseer-WebUI
A simple asicseer webui interface.

Please run the following to install dependencies:
`pip install flask`

To run:
`python main.py`

Im not a professional flask developer so take this with a grain of salt. Im not developing this any further so if you need to make changes please make them yourself.
